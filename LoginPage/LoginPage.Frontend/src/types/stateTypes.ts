export type Users = {
  id: number,
  username: string,
  password: string,
  email: string | null,
}[];

export type User = {
  id: number,
  username: string,
  password: string,
  email: string | null,
};